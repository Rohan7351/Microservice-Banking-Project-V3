server:
  port: 8071

spring:
  application:
    name: "configserver"
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
#          search-locations: "classpath://config"
#          search-locations: "file:///C://Users//rohan//OneDrive//Desktop//Microservice Lerning V2//configserver//src//main//resources//config"
          uri: "https://github.com/Rohan7351/microservice-config"
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

encrypt:
  key: "CU8W3UVLEFCXVAPK6IYCK4QM3UU8XWTI"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
